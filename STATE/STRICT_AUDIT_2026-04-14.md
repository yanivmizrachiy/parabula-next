# STRICT AUDIT — 2026-04-14

מטרת מסמך זה: לקבע בתוך הריפו עצמו את תמונת המצב המחמירה והמחייבת לפי דרישות המשתמש, בלי ערבוב בין מה שחי, מה שדורש ניקוי, ומה שאסור לשנות.

## דרישות מחייבות של המשתמש

- לעבוד חכם, מסודר, וללא בלגן.
- למחוק מהריפו מה שלא צריך.
- להשאיר טוב את מה שכן צריך.
- לעדכן תמיד את דף הכללים לפי הדרישות האמיתיות של המשתמש.
- בדפי **משוואות**: לא לשנות מלל, אלא לתקן רק עיצוב.
- בדפי **משוואות**: לנקות כותרות מלוכלכות, שאריות עיצוב ישנות, חוסר אחידות בפונטים, וריווחים לא עקביים.
- לשמור על אחידות עם שפת העיצוב של הפרויקט.
- לאפשר כניסה נוחה וקבועה לנייד עם אייקון לאתר/לאפליקציית הצפייה בדפים המעודכנים.

## מה אומת בפועל

### 1. שכבת המובייל קיימת וחיה

הקבצים הבאים קיימים ומשמשים כשכבת כניסה לנייד:

- `preview/phone.html`
- `preview/install.html`
- `preview/manifest.webmanifest`
- `preview/icon.svg`

### 2. קיימת סתירה חיה בין הכללים לבין הקוד

- `PROJECT_RULES.md` אוסר `<style>` inline ודורש הפרדה מלאה בין HTML ל-CSS.
- `preview/app.html` עדיין מכיל `<style>` פנימי גדול.

### 3. קיימת סתירה תיעודית חיה

- `preview/README.md` עדיין מתעד `print-center.js` וכפילות הדפסה.
- `preview/APP_CONTRACT.md` עדיין מתעד `print-center.js` כמצב פתוח.

### 4. דפי משוואות דורשים תיקון רוחבי, לא טלאים

לפי בדיקה רוחבית, הבעיה בתחום זה היא ברמת אחידות העיצוב, לא ברמת תוכן. לכן כיוון העבודה המחייב הוא:

- תיקון עיצוב רוחבי בטוח
- בלי שינוי מלל
- בלי המצאת תוכן
- עם מחיקה של שאריות עיצוב ישנות שלא נדרשות יותר

## מה חייב להתעדכן בהמשך

### A. חובה לעדכן

1. `PROJECT_RULES.md`
   - להבהיר שמותר לבצע **design-only shared cleanup** למשפחות דפים כמו `משוואות`, כל עוד לא משנים תוכן.
   - להבהיר ששכבת המובייל/האייקון לנייד היא חלק רשמי מהמערכת החיה.
   - להחיל במפורש את כלל no-inline-style גם על `preview/`.

2. `preview/app.html`
   - להוציא את כל ה-inline styles לקובץ CSS ייעודי.

3. `preview/README.md`
   - למחוק אזכורים ל-`print-center.js` אם הוא כבר לא חלק מהשכבה הפעילה.
   - לעדכן שההתקנה לנייד היא חלק רשמי מהמערכת.

4. `preview/APP_CONTRACT.md`
   - למחוק אזכורים היסטוריים שלא מייצגים את המצב החי.
   - לקבע ש-`print.js` הוא קובץ ההדפסה הקנוני הפעיל.

### B. חובה למחוק/לנקות

- כל אזכור תיעודי חי ל-`print-center.js`, אם הקובץ כבר לא משמש את השכבה הפעילה.
- כל שאריות עיצוב ישנות בדפי משוואות שלא נדרשות למבנה החי.
- כל `<style>` inline בשכבת `preview/`.

## מה אסור לשנות

- מלל בדפי משוואות.
- תוכן לימודי.
- מבנה A4 הקנוני של דפי `עמוד-N.html` מעבר למה שנדרש כדי לשמור על אחידות עיצובית בטוחה.

## עקרון הכרעה מחייב

אם יש סתירה בין קוד, תיעוד, ודף הכללים — יש ליישר את שלושתם כך שישקפו את **המצב החי** ואת דרישות המשתמש, ולא להשאיר סתירות פתוחות.

## סטטוס

- האבחון המחמיר הושלם.
- הכתיבה הישירה לחלק מהקבצים החיים נחסמה בכלי החיצוני בזמן audit זה.
- מסמך זה נוצר כדי לוודא שהמשך העבודה לא יאבד וששום AI עתידי לא יפספס את הדרישות המחייבות.
