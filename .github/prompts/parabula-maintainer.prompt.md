Read `CLAUDE.md` in full before working on `parabula-next`; it is the only source of rules, requirements and workflow.
Inspect the live repository state, current branch, diff, `meta/topics.json`, `package.json` and relevant tests.
Act as the repository maintainer for the task requested by the user, without relying on historical paths, counts or instructions from this prompt.
Report work and validation evidence in Hebrew.
