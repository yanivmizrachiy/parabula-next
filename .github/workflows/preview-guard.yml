name: Preview Guard

on:
  push:
    branches: ["main"]
    paths:
      - 'preview/**'
      - 'scripts/preview-guard.mjs'
      - '.github/workflows/preview-guard.yml'
  pull_request:
    branches: ["main"]
    paths:
      - 'preview/**'
      - 'scripts/preview-guard.mjs'
      - '.github/workflows/preview-guard.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Run preview guard
        run: node scripts/preview-guard.mjs
      - name: Upload preview guard report
        uses: actions/upload-artifact@v4
        with:
          name: preview-guard-report
          path: meta/audit/preview-guard.json
