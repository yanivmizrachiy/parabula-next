# CLAUDE.md — Parabula Next — מקור הכללים היחיד

## תוכן העניינים

- **[§0 — סמכות המסמך](#0-סמכות-המסמך)** — מקור הכללים, הדרישות והזיכרון היחיד; קובץ זה גובר.
- **[§1 — דרך העבודה](#1-דרך-העבודה)** — מהות הפרויקט (שכפול נאמן, אין חילול תוכן) + כללי העבודה.
- **[§2 — מקורות אמת וארכיטקטורה](#2-מקורות-אמת-וארכיטקטורה)** — קבצים קנוניים, קוראים, פריסה ל-GitHub Pages.
- **[§3 — חוזה דף העבודה והדפסה](#3-חוזה-דף-העבודה-והדפסה)** — A4 210×297, RTL, MathJax, בלי inline CSS.
- **[§4 — תוכן מתמטי ועיצוב דפים](#4-תוכן-מתמטי-ועיצוב-דפים)** — נאמנות למקור, בלי מספור שאלות, חוזי נושאים (§4.1–§4.6).
- **[§5 — תצוגה ושימוש בטלפון נייד](#5-תצוגה-ושימוש-בטלפון-נייד--חוזה-מחייב)** — שוויון מלא נייד↔מחשב, PWA (§5.1–§5.10).
- **[§6 — מטא־דאטה, build וגרסאות](#6-מטאדאטה-build-וגרסאות)** — `topics.json`, גרסה אחת מה-build, cache-bust ל-MathJax/CSS.
- **[§7 — בדיקות מחייבות](#7-בדיקות-מחייבות)** — שערי האיכות שרצים לפני release.
- **[§8 — Git ושינויים מסוכנים](#8-git-ושינויים-מסוכנים)** — ענף ייעודי, commit ממוקד, בלי פעולות הרסניות.
- **[§9 — ממשל תיעוד ו־AI](#9-ממשל-תיעוד-ו-ai)** — מקור-כללים יחיד נאכף אוטומטית.
- **[§10 — פתיחת כל סשן AI](#10-פתיחת-כל-סשן-ai)** — צ'קליסט הפתיחה של כל סשן.

---

## 0. סמכות המסמך

זהו **קובץ ההוראות, הדרישות והזיכרון המחייב היחיד** של `parabula-next`.

- כל AI, סוכן, prompt, command או כלי אוטומציה חייב לקרוא קובץ זה במלואו לפני פעולה.
- אין ליצור או לתחזק קובץ כללים, memory, contract או instructions נוסף.
- `.claude/`, `.github/prompts/`, `README.md`, `docs/` ו־`preview/` רשאים להפנות לכאן בלבד; אסור להם לשכפל כללים.
- מצב משתנה, ספירת דפים, תוצאות בדיקות והיסטוריית עבודה אינם נשמרים כאן. בודקים אותם מהקוד ומהנתונים בזמן אמת.
- במקרה של סתירה: קובץ זה גובר, והמקור הסותר מתוקן או נמחק.

---

## 1. דרך העבודה

**מהות הפרויקט (הדרישה העליונה של יניב):** יניב מספק חומר מקור — תמונות, PDF, טקסט או קבצים — והמערכת **משכפלת אותו בנאמנות מלאה** לדף עבודה A4 מעוצב לפי כללי העיצוב הקבועים (§3–§4). המערכת **לעולם אינה מחוללת, ממציאה או מוסיפה** שאלות, תרגילים, תשובות, נתונים, סרטוטים או תוכן לימודי שאינו במקור, ואין להציע מחוללי תרגילים/גרפים/משימות. **חריגים מפורשים בלבד:** „גאומטריה ז” במצב „מקור + העשרה” (§4.2), „משפט פיתגורס” במצב „יסודות חדשים + רצף קיים” (§4.2א), ו„משוואות ריבועיות” במצב „כותרות מקור + מאגר חדש מדורג” (§4.5). אין להסיק מן החריגים הרשאה לנושא אחר.

1. אמת את מצב הריפו, הענף וה־diff לפני שינוי.
2. קרא את הקבצים בפועל; אל תניח שמסמך ישן מתאר את המצב הנוכחי.
3. השתמש בתוכן אמיתי בלבד. אין דמה, placeholder, כפתור מזויף, נתון מומצא או טענה שלא נבדקה.
4. בצע את השינוי הקטן והמערכתי ביותר שפותר את שורש הבעיה.
5. אל תשבור יכולת קיימת כדי לשפר משטח אחר.
6. כל רגרסיה שנמצאה חייבת להפוך לבדיקה אוטומטית קבועה.
7. אין להכריז על השלמה בלי לציין בדיוק מה נבדק ומה לא נבדק.
8. תקשורת ודוחות למשתמש בעברית, אלא אם ביקש אחרת.

---

## 2. מקורות אמת וארכיטקטורה

- דפי העבודה הקנוניים: `עמוד-N.html` בשורש.
- CSS משותף A4: `styles/a4-base.css`.
- CSS ייעודי לדף: `styles/pages/עמוד-N.css` או שכבת נושא פעילה ומוגדרת.
- מטא־דאטה וניווט: `meta/topics.json` בלבד. הקובץ מכיל גם את עץ תכנית הלימודים (§4.5) וגם את מערך `topics` השטוח.
- קורא מחשב: `catalog.html`, `catalog.js`, `catalog.css`.
- קורא נייד: `mobile-app.html`, `mobile-app.js`, `mobile-app.css` בלבד.
- פעולות הדפסה, PDF, הורדה ובחירה: `reader-actions.js` ו־`reader-actions.css`.
- `/preview` הוא משטח פיתוח מקומי בלבד: `preview/index.html`,‏ `preview/server.mjs` ו־`styles/preview.css`. הוא קורא את `meta/topics.json` הקנוני, ואינו נושא כללים משלו ואינו מקור אמת. הוא אינו חלק מהאתר המפורסם. **הניווט נשאר גלוי**: רשימת הנושאים, סינון הנושא, החיפוש, כפתורי קודם/הבא וה־viewer חייבים להישאר במסך — אסור להסתיר את סרגל הצד או להסתמך על הסתרתו.
- **רצועת `topic-link` בדף עבודה** (`preview-nav-topics`) היא שריד מעידן הנושאים השטוחים, וגודלה אינו אחיד. הניווט המלא לפי תכנית הלימודים נמצא בקוראים (§4.5), ולכן אין דרישה למספר קישורים מסוים. הדרישה היא שהרצועה קיימת בכל דף, ושכל `topic-link` מצביע על קובץ `עמוד-N.html` שקיים בפועל.
- **שרשרת „הקודם/הבא" בדפי העבודה היא רציפה על פני כל הספר**: היא עוברת בין נושאים לפי סדר הנושאים ברצועה ואז לפי המספור המקומי, ורק הדף הראשון והאחרון בספר נושאים כפתור מושבת. מחולל דפים שמחשב שכן חייב למיין לפי המספור המקומי — סדר המערך ב־`meta/topics.json` הוא סדר הכנסה ואינו סדר קריאה.
- PWA נייד: `mobile-app.webmanifest` ו־`sw.js`.
- אין metadata mirror, אין מסלול mobile נוסף ואין שכפול דפי עבודה לצורך נייד.
- **פריסה:** `.github/workflows/deploy-pages.yml` בונה את `dist/` ומפרסם ל־GitHub Pages דרך **GitHub Actions**. מקור ה־Pages בהגדרות הריפו חייב להישאר „GitHub Actions” ולעולם לא „Deploy from a branch → /docs” — הגדרה שגויה מפילה את **כל** האתר ל־404. GitHub `origin/main` הוא מקור האמת; המקומי מסונכרן אליו וה־deploy אוטומטי.

קבצים ומסלולים אסורים כוללים `mobile-topics.json`,‏ `mobile.css`,‏ `preview/phone.*`,‏ `preview/mobile-app.*`,‏ `mobile-app-install.*` וכל חלופה פעילה למסלול הקנוני.


### 2.1 חוזה קנוני — מעגל, גליל וחרוט

הוראה מפורשת של יניב (2026-08-18) לשלושת הנושאים האלה בלבד. סעיף זה הוא חריג תחום מפורש למבנה הכללי של `עמוד-N.html` ואינו משנה שום נושא אחר בריפו.

- **מקור אמת יחיד:** כל האתר, החוברות, הנכסים והקוד הפעיל של **מעגל, גליל וחרוט** נמצאים ב־`yanivmizrachiy/razpages` בלבד, תחת `workbooks/`. אין לתחזק עותק פעיל מקביל בריפו אחר.
- **שורש קנוני:** `workbooks/`. הרישום הטכני הוא `workbooks/manifest.json`; הוא נגזר ונבדק מן הקבצים ואינו מסמך כללים נוסף. מספרי הדפים אינם נכתבים כאן ידנית אלא נקראים בזמן אמת מן הרישום/הקבצים.
- **האתר המשותף הקנוני:** `workbooks/index.html` הוא דף הבית היחיד לשלוש החוברות. הוא משמר את שפת העיצוב וההתנהגות שכבר פותחו בהשראת אתר זוויות: בחירת מעגל/גליל/חרוט, חוברת רציפה, ניווט מהיר, הדפסה מלאה והתאמה לנייד. אין ליצור דף בית מתחרה או viewer חלופי לשלישייה.
- **חוברות קנוניות:** `workbooks/circle/`, `workbooks/cylinder/`, `workbooks/cone/`. כל חוברת מתחילה במספור מקומי 1 וממשיכה ברצף עד סופה; אין לערבב את המספור הגלובלי של שאר `razpages` במספור שהתלמיד רואה בחוברות האלה.
- **מעגל:** החוברת נפתחת מן היסודות — מעגל לעומת עיגול, מרכז, רדיוס וקוטר — וממשיכה כרצף אחד. אין להשאיר חוברת מעגל נוספת או סדר חלופי פעיל.
- **גליל:** כל דפי הגליל נשמרים כחוברת אחת רציפה תחת `workbooks/cylinder/` ומשתמשים באותו אתר משותף ובאותם חוזי צפייה/הדפסה.
- **חרוט:** כל דפי החרוט, המקורות, ההמחשות והחומרים המוגנים עוברים תחת `workbooks/cone/`. חומר מקור נעול נשמר בנאמנות מלאה; בפרט דף המקור של איילת קריספין אינו מנוסח מחדש או מוחלף, ומותר בו שדרוג חזותי בלבד שאינו משנה תוכן, ציור, סדר או משמעות.
- **אין כפילויות:** לאחר אימות ההעברה, אין להשאיר ב־`razpages` שתי גרסאות פעילות של אותו אתר, viewer, דף או חוברת. קובץ מעבר זמני יכול להתקיים רק במהלך מיגרציה ונמחק לפני השלמה.
- **אין תלות בריפו הישן:** לאחר אימות parity מלא ומחיקת הריפו הישן, אסור לקוד, CSS, תמונה, README, בדיקה או build להפנות ל־`smartschool-hebrew-voice-notes`. כל asset שהיה נטען משם חייב להיות מקומי ב־`razpages`.
- **חוזי הפרויקט נשמרים:** A4 אמיתי, RTL, קרדיטים, דיוק מתמטי, נגישות, מובייל, הדפסה ו־GitHub Pages כפופים לסעיפים הכלליים של `CLAUDE.md`. החריג כאן הוא מיקום החוברות והאתר תחת `workbooks/`, לא ויתור על איכות או בדיקות.
- **מחיקה היא השלב האחרון בלבד:** הריפו הישן נמחק לצמיתות רק אחרי שהועברו כל הקבצים הרלוונטיים, הוסר כל קישור חיצוני אליו, האתר הקנוני נבנה בהצלחה, וכל שלוש החוברות נפתחות ונבדקות מן `razpages` בלבד.

---

## 3. חוזה דף העבודה והדפסה

- **המוצר הוא דף עבודה להדפסה.** האתר, הקורא והגלילה הם אמצעי צפייה, בדיקה, ניווט והדפסה בלבד; החלטות עיצוב ותוכן מוכרעות לפי איכות דף ה־A4 המודפס והתלמיד הכותב עליו ביד, ולא לפי הצורך „למלא מסך”.
- גודל קנוני: A4 ‏`210mm × 297mm`.
- אסור לשנות את חוזה A4 או להסתיר overflow כפתרון לתוכן שלא נכנס.
- אין `overflow:auto` או `overflow:scroll` בתוך דף A4.
- אין CSS inline בדפי `עמוד-N.html`: לא `<style>` ולא `style="..."`.
- ההדפסה חייבת לשמור על A4 אמיתי, RTL, MathJax, SVG, גופנים ושבירת עמודים נכונה.
- תצוגת מחשב או נייד רשאית לבצע scale חיצוני בלבד; אסור להקטין או לעוות את מקור A4.
- שינוי בקורא, בהדפסה או במובייל אינו רשאי לשנות את תוכן הדף או את גאומטריית ההדפסה.

---

## 4. תוכן מתמטי ועיצוב דפים

- עברית RTL; קטע LTR מוגדר ב־CSS רק במקום שנדרש.
- MathJax: ‏`\(...\)` לנוסחה בשורה ו־`$$...$$` לנוסחה מוצגת; אין `$...$` יחיד.
- **אחידות גופן מתמטי — כלל גורף (הוראת יניב, 2026-08-18):** כל ביטוי מתמטי שהתלמיד רואה מוצג בגופני ה־TeX של MathJax הנטענים מ־`vendor/` — אותו גופן בכל הפרויקט, כמו בחוברת משוואות ריבועיות. ביטוי בשורת טקסט נכתב כ־MathJax (`\(...\)`); תווית מידה בתוך SVG משתמשת ב־`@font-face` שמפנה לאותם קובצי woff2 (‏`mjx-tex-n` למספרים זקופים, `mjx-tex-mi` למשתנים באיטליק מתמטי). אסור לכתוב תשובה/נוסחה כטקסט רגיל עם סוגריים במקום רינדור מתמטי, ואסור להציג מתמטיקה בגופן הממשק.
- **כפל — כלל ברזל (הוראת יניב, 2026-08-18):** סימן כפל שמוצג לתלמיד הוא **נקודה אמצעית `·` בלבד**. ב־MathJax משתמשים ב־`\cdot`. אסור להשתמש ב־`×`, ב־`\times`, או באותיות `x`/`X` כסימן כפל. האות `x` מותרת רק כאות/נעלם מתמטי. הכלל חל בכל דפי ההדפסה ובכל דוגמה מתמטית חדשה בפרויקט.
- אין לנחש טקסט, נתון, תשובה, מידה או סרטוט מתמטי.
- תרשים מתמטי חדש נבנה כ־SVG/HTML וקטורי חד ובר־עריכה, עם strokes שאינם מתעוותים.
- צילום או איור דקורטיבי שמקור השאלה מפנה אליו יכול להישאר raster; אין להמירו ל־SVG מומצא.
- יש לשמור על נאמנות למקור, קריאות, ניצול A4 ויכולת עריכה עתידית.
- כותרות, מספור נושא ומספור מקומי נגזרים מ־`meta/topics.json`, לא מרשימה קשיחה.
- **אין מספור שאלות גלוי בדף עבודה.** הדף נושא רק את מספר העמוד שבכותרת; אסור להוסיף כותרות „שאלה N” או מספור שאלות. שאלות ותתי־שאלות מסומנות בכדור שחור; אותיות סעיף (א/ב/ג) מותרות כתווית סעיף בלבד. מספרי רב־ברירה או תוויות התאמה שהם פונקציונליים נשמרים.
- **המספור הגלוי היחיד הוא המספר המקומי בנושא (הוראת יניב, 2026-07-19):** מספר העמוד מופיע למעלה משמאל בדף בלבד. אסור להציג בקוראים, בכרטיסי דפים או בשורות התקדמות את המספר הגלובלי של הקובץ („עמוד 169”, „עמוד N מתוך 560 בספר”) — הוא זהות קובץ פנימית, לא מספור לתלמיד. התקדמות מוצגת במספור מקומי בנושא בלבד.
- **נאמנות מוחלטת למקור:** מעתיקים מלל, מספרים ותשובות בדיוק כפי שבמקור, גם כשהם נראים לא־עקביים. תיקון טעות שקיימת במקור מותר רק באישור מפורש של יניב.
- **תוארו של יניב רז — כלל גורף בכל הפרויקט (הוראת יניב, 2026-07-18):** בכל מקום שבו מופיע השם „יניב רז” — דף עבודה, כותרת תחתית, קרדיט, קורא, מצגת, אתר, README או תיעוד — הוא מופיע עם התואר המלא **`מדריך מחוזי חט"ב בעיר ירושלים`**. אין לכתוב „מדריך חט״ב בעיר ירושלים”, „מדריך מחוזי בעיר ירושלים” או את השם בלי תואר. מפריד (`-` או `·`) נשמר לפי הסגנון הקיים באותו משטח; רק נוסח התואר אחיד.
- **קרדיט קבוע בכל דף עבודה — כלל גורף (הוראת יניב, 2026-07-19):** בכל `עמוד-N.html` ללא יוצא מן הכלל מופיעה כותרת תחתית בת שתי שורות, בתוך אלמנט `<footer>` יחיד, בנוסח המדויק הזה ובסדר הזה:
  - `יניב רז - מדריך מחוזי חט"ב בעיר ירושלים`
  - `הדרכה במחוז ירושלים והעיר ירושלים - מנח"י, בהובלת איילת קריספין`

  השורה השנייה נמצאת **מתחת** לראשונה, לא לצידה. הסגנון הקנוני יושב ב־`styles/a4-base.css` תחת `.a4-page > .gz-footer` (`.f1`/`.f2`); שכבת נושא רשאית לדרוס את הסגנון בלבד — לא את הנוסח, לא את הסדר ולא את עצם קיומו. כל מחולל דפים חייב לפלוט את הקרדיט, ו־`tests/contracts/page-credit.test.mjs` נכשל על כל דף חסר.
- **אין תוויות קושי, שלב או סוג־שאלה גלויות — כלל גורף (הוראת יניב, 2026-07-19):** מילים כמו „תרגול”, „העשרה”, „אתגר”, „ביסוס”, „שלב ביניים”, „שילוב כלים”, „העמקה”, „למידה מודרכת” ו„שלב N” הן תוויות דמו ואסורות בכל טקסט שהתלמיד רואה — כותרת, תת־כותרת, eyebrow, תגית, כותרת שאלה או קידומת מודגשת. המדרג הפדגוגי נשמר בנתונים הפנימיים בלבד וקובע סדר בלבד (§4.5). כותרת שאלה מתארת את **תוכן** השאלה, לא את רמתה. חריג יחיד: מילה שהיא חלק אמיתי ובלתי נפרד מתוכן השאלה במקור (למשל „אתגר התקציב” כשם תרחיש בשאלה אוריינית) — ואז היא נשמרת בנאמנות למקור.
- **ניצול A4 מאוזן — מלא אך לא צפוף (הוראת יניב, 2026-08-17):** ניצול טוב של עמוד אינו פירושו לדחוס כמה שיותר תרגילים. הדף צריך להרגיש מלא, נוח ומרווח: ציורים אינם מנופחים כדי למלא שטח, אך גם אין אזורים גדולים ומתים ללא תפקיד לימודי. מרווח לבן נשמר כאשר הוא תורם להפרדה, לקריאות או למקום כתיבה. מספר התרגילים, גודל השרטוטים והמרווחים נקבעים יחד לפי שיקול דעת דידקטי; אין לפתור עמוד ריק באמצעות צפיפות, הקטנת גופן או אוסף אקראי של מיני־משימות.
- **תיבת מענה קנונית יחידה — „סגנון 5” (הוראת יניב, 2026-08-17):** כאשר נדרשת תיבת השלמה או תשובה מלבנית, משתמשים באותו סגנון בכל `razpages`: רקע לבן, מסגרת עדינה `1.35px solid #1f2a44`, פינות מעוגלות `8px`, גובה בסיס `30px` וצל תלת־ממדי עדין ולא כבד. אין ליצור וריאציות מקומיות מתחרות. **הרוחב אינו קבוע**: הוא נגזר מהתשובה שהלומד אמור לכתוב — מספר קצר מקבל תיבה צרה, מילה קצרה תיבה מעט רחבה יותר, מילה או ביטוי בינוני תיבה בינונית, וביטוי ארוך תיבה רחבה. אין לתת לתשובה קצרה תיבה ענקית ואין לחנוק תשובה ארוכה בתיבה קטנה. כאשר התשובה מורכבת מכמה ערכים, נשמר מבנה התשובה לפי §4.3 ולא מחליפים אותו במלבן יחיד ארוך.
- **השלמה מודרכת משאירה רק את הידע הנלמד חסר (הוראת יניב, 2026-08-17):** במשפט הסבר או הגדרה לא מוחקים באקראי טקסט כדי „לייצר פעילות”. משאירים לתלמיד להשלים רק את המספר, המושג או הקשר המרכזי שאותו רוצים שיישלף מהזיכרון. יחידות, סימן מעלות, אופרטורים, סימני פיסוק והתחביר שאינם מטרת הלמידה נשארים מודפסים מחוץ לתיבה. לדוגמה, בהשלמת גודל של זווית ישרה התלמיד משלים `90`, וסימן `°` כבר נתון.
- **רצף תרגול דידקטי — אין פריט בודד מסוג חדש (הוראת יניב, 2026-08-17):** בתוכן חדש, בהעשרה או בנושא שבו מותר שיפור פדגוגי, פורמט משימה חדש אינו מוצג פעם אחת בלבד ואז ננטש. זיהוי, סיווג, השלמה, סימון, כתיבה או דפוס מענה חדש מוצגים תחילה כמקבץ קטן ועקבי של פריטים מקבילים, כדי שהתלמיד יבין את הדרישה, ישווה ויתרגל. **ברירת המחדל היא לפחות 3 פריטים מקבילים**; שני פריטים מותרים כאשר ההשוואה הזוגית עצמה היא המטרה. לאחר שהדפוס התבסס אפשר לעבור לפורמט הבא. משימת סינתזה או העברה שמחברת מיומנויות שכבר תורגלו יכולה להיות פריט יחיד. אין למלא עמוד באוסף אקראי של משימה אחת מכל סוג רק כדי לנצל שטח. בנושאים שמחויבים לנאמנות מוחלטת למקור אין להמציא פריטי מקור חדשים רק כדי לעמוד בכלל זה; הכלל חל במקום שבו מותר ליצור, להעשיר או לשפר תוכן.

### 4.1 חוזי משוואות פעילים

- `משוואות ריבועיות` הוא נושא נפרד לחלוטין ממשפחת `משוואות`; אין להעביר ביניהם עיצוב או תוכן בלי הוראה מפורשת.
- `עמוד-95.html` הוא דף מקור רגיש: יש לשמור את הביטוי `4 + x = \square` ואת מבנה התרגילים/תשובות אלא אם קיימת ראיית מקור אחרת.
- `עמוד-42.html` ו־`עמוד-43.html` נשמרים נאמנים למקור; אין בנייה מחדש או המרה מתוך ניחוש.
- כלי audit ואוטומציה של משוואות הם `read-only` ואינם משנים דפי עבודה.
- `.github/workflows/equations-guard.yml` נשאר guard קריאה בלבד.

### 4.2 חוזה נושא „גאומטריה ז” (מצב „מקור + העשרה”)

הוראה מפורשת של יניב (2026-07-18) לנושא זה בלבד:

- **מקור:** מסמך תוכנית הלימודים „גאומטריה קדם־היסקית לכיתה ז” (הקובץ `גאומטריה 1.pdf` שסיפק יניב). כל שאלות המקור מועתקות אליו בנאמנות מלאה — מלל, נתונים, אותיות וסרטוטים — לפי כללי §4.
- **העשרה מקורית:** לצד שאלות המקור נכתבות שאלות תרגול מקוריות באותו תת־נושא, שונות באמת (לא שכפול), לפי ההערות הדידקטיות של המסמך (שאלות עם יותר מפתרון אחד/ללא פתרון, „מי צודק”, מנחים שונים, קיפולי נייר, אומדן, קישור לחיי היום־יום ולמערכת צירים, חישוב ואלגברה). שאלת העשרה לעולם אינה מוצגת כשאלת מקור.
- **ניצול עמוד:** כל דף A4 מנוצל במלואו — שאלת מקור אחת לפחות + שאלות העשרה, עם מקום מענה מתאים (שורות/מסגרות/מערכות צירים). שאלה שחסר לה סרטוט במקור מקבלת סרטוט וקטורי מדויק ומועיל.
- **מספור:** שם הנושא ב־`meta/topics.json` הוא `גאומטריה ז`; הכותרת המקומית מתחילה מ„עמוד 1” ונמשכת ברצף. שמות הקבצים ממשיכים את המספור הגלובלי של הריפו.
- **כותרת תחתית קבועה:** לפי הכלל הגורף ב־§4 — אותן שתי שורות בדיוק, בכל דף בריפו.
- **עיצוב:** שכבת נושא `styles/topics/geometry7.css` בתחום scope‏ `.geo7-page`, באותה שפה חזותית של שכבת `angle7` (כרטיסי שאלה, כדור שחור, SVG וקטורי חד).
- סדר הדפים עוקב אחר סדר הפרקים במסמך המקור; ההתקדמות נבדקת מ־`meta/topics.json` מול המקור, לא מרשימה קשיחה כאן.
- **אין תווית „העשרה” גלויה** (הוראת יניב, 2026-07-19). ההבחנה בין שאלת מקור לשאלת העשרה היא כלל עבודה פנימי בלבד; אסור להדפיס אותה על הדף בשום צורה — לא תגית, לא כותרת, לא צבע ולא סימון. התלמיד רואה שאלות בלבד.

### 4.2א חוזה נושא „משפט פיתגורס” (מצב „יסודות חדשים + רצף קיים”)

הוראה מפורשת של יניב (2026-08-16; עודכן לפי הערות ההדפסה 2026-08-18) לנושא זה בלבד:

- **מקור אמת יחיד:** כל דרישות פיתגורס המחייבות נכתבות רק כאן, בתוך `CLAUDE.md`. אין ליצור מסמך כללים, memory, contract או brief מקביל. בדיקות אוטומטיות רשאיות לאכוף את הכללים אך אינן מקור אמת נוסף.
- **כל החומר הקיים נשמר:** דפי פיתגורס הוותיקים `עמוד-9.html` עד `עמוד-30.html` ו־`עמוד-41.html` נשארים חלק קבוע מהנושא ואינם חומר זמני או מועמד למחיקה. גם דפי היישום המתקדמים `עמוד-375.html` עד `עמוד-380.html`, המשויכים לצומת פיתגורס בתכנית הלימודים, נשמרים. דפי יסוד חדשים מצטרפים אליהם; אין להחליף את הפרויקט הוותיק בחוברת חדשה.
- **שיפור תוספתי כברירת מחדל:** מותר ליצור, להוסיף ולשפר תוכן יסודות חדש ומדורג. אין למחוק דף תקין, שאלה מועילה או מיומנות קיימת רק כדי להכניס הוראה חדשה; מוסיפים, משפרים ומתקנים, ורק תוכן שגוי או סותר מוחלף. העריכות העתידיות חלות באופן אחיד על כל רצף פיתגורס לפי כללי הפרויקט.
- **מספר הדפים אינו יעד ואינו מגבלה:** מספר דפי פיתגורס נגזר מכמות הכתיבה, הנימוק והתרגול שהתלמיד צריך. אם אין מקום אמיתי — מפצלים ומוסיפים דפי A4. אסור לדחוס רק כדי לשמור על 43 דפים או על מספר היסטורי אחר. כל ספירה מוצגת נגזרת בזמן אמת מן המטא־דאטה והניווט.
- **סדר היסודות המחייב:** זווית ישרה → משולש ישר־זווית → ניצבים → יתר → זיהוי ניצבים ויתר גם בסיבוב → חזקה שנייה וריבועי מספרים → שורש ריבועי מושלם → פתרון משוואות אורך מהצורה \(x^2=n\) במספרים בעלי שורש שלם → שורשים שאינם שלמים וקירוב → פתרון \(x^2=n\) במספרים שאינם בעלי שורש שלם → משמעות גאומטרית של ריבוע הצלע → גילוי וניסוח משפט פיתגורס → \(a^2+b^2=c^2\) → כתיבת משוואה → דרך פתרון מלאה למציאת יתר → מציאת יתר במשולש → דרך פתרון למציאת ניצב → תרגול משולב. מספר הדפים נגזר מאיכות ההוראה ואינו מכסה קשיחה.
- **כותרות קבוצתיות — מעט כותרות (הוראת יניב, 2026-08-18):** אין שם חדש כמעט לכל עמוד. דפי היסוד מחולקים למשפחות גדולות: עמודים 1–5 נושאים את הכותרת **„מושגים בסיסיים”**, עמודים 6–12 את **„חזקות ושורשים”**, ומעמוד 13 ואילך את **„משפט פיתגורס”**. המיומנות הספציפית מופיעה בהוראה ובתוכן, לא בכותרת H1 חדשה. דף נוסף שנוצר מפיצול יורש את כותרת המשפחה שלו.
- **השלמה עליונה משמעותית (הוראת יניב, 2026-08-18):** בראש דף שמציג או מחזק ידע מרכזי, משפט ההסבר אינו נותן אוטומטית את כל מילת המפתח. משאירים לתלמיד השלמה מודרכת של המושג/המספר המרכזי, לפי כלל §4 „השלמה מודרכת”; אין ליצור חור מלאכותי בטקסט שאינו ידע נלמד.
- **מושגים בסיסיים לפני חישוב:** התלמיד חייב להכיר ולתרגל זווית ישרה, משולש ישר־זווית, ניצבים, יתר, חזקת 2, ריבועים, שורש ריבועי והקשר בין ריבוע לאורך צלע לפני פתרון מלא של משפט פיתגורס.
- **היתר — שתי עובדות שחייבים ללמד ולתרגל:** היתר נמצא מול הזווית הישרה והוא הצלע הארוכה ביותר במשולש ישר־זווית. התלמיד מתרגל זיהוי יתר במשולשים בכיוונים שונים וגם מתוך אורכי צלעות, כדי שלא יזהה יתר לפי „צלע אלכסונית” או מיקום קבוע בציור.
- **אין רמז שמגלה את התשובה (הוראת יניב, 2026-08-18):** במשימת זיהוי של זווית ישרה או משולש ישר־זווית אין לצייר מראש ריבוע 90° בתוך אפשרויות הבחירה ואין לכתוב תווית נגישות שחושפת את התשובה. קודם התלמיד מזהה; לאחר מכן אפשר לדרוש ממנו להוסיף את סימון הזווית הישרה בעצמו. `aria-label` במשימת זיהוי נשאר ניטרלי.
- **תקן עיצוב והוראה מרוכז (הוראות יניב, 2026-08-18, סבב הביקורת החי):**
  - **שרטוטים גדולים** שממלאים את המסגרת; אין לכווץ שרטוט כדי לדחוס תוכן. בכל שרטוט זווית מסומנת **נקודת קודקוד**.
  - **מקבץ זיהוי/סיווג זוויות** — כלל מסוכם (הוראת יניב, 2026-08-18): **אין מספור אותיות** (א/ב/ג) על הזוויות שבמקבץ. הזוויות **מגוונות ומעניינות**: כיוונים שונים כולל **הפוכות/מסובבות** (פתיחה למעלה, למטה, באלכסון), ו**אורכי קרניים משתנים** — חלק ארוכות וחלק קצרות, לא כולן באותו אורך. כל זווית עם **נקודת קודקוד** ותיבת מענה מתחתיה. כל זווית מחושבת ומאומתת: **בדיוק 2 זוויות ישרות ‎90.0°‎** ו־**3 מסיחים מחוץ לטווח ‎78°–102°‎**. אותו עיקרון חל על מקבצי זיהוי משולשים.
  - **הוראה אחת לכל סעיף**, בפורמט אחיד: מתחילה מימין עם כדור שחור ובגופן ההוראות הרגיל. אין כותרות־משנה או הוראות ביניים („השלימו ובצעו”) ואין כותרת ממורכזת מודגשת בתוך כרטיס. מונחים נכתבים במלואם („זווית ישרה”, לא „ישרה”).
  - **מסגרות ציור** (השלמת קרן/צלע): בלי אותיות סימון (א/ב) כשאין הפניה טקסטואלית אליהן; מסגרת מודגשת `1.5px dashed #94a3b8`; הצורה ממורכזת במסגרת, לא שוכבת בתחתיתה.
  - **שתי מסגרות השלמה אינן אותה משימה:** מדרגים — למשל בראשונה חסר היתר (הצלעות הנתונות נפגשות בזווית הישרה) ובשנייה נתונים יתר וניצב והתלמיד קובע היכן תיווצר הזווית הישרה.
  - **השלמה עליונה אינה חוזרת** על השלמה שכבר בוצעה בעמוד קודם; משלימים את הידע החדש של העמוד, ומותר שתי השלמות באותו משפט (למשל „ישר” ו„ישרה” כשה־‎90°‎ כבר מודפס).
  - **הקרדיט התחתון** מונמך מעט (`translateY(9px)`) כדי לשבת ממורכז בגובה שולי הדף.
  - **אין שטח מת בתחתית העמוד:** `question-block` נשאר `space-between` כך שהתוכן נמתח על גובה העמוד; תיקון שמפנה מקום חייב להימדד גם ב־deadGap (המרחק בין תחתית התוכן לקרדיט ≤ ‎40px‎), לא רק בהיעדר גלישה.
  - כל שינוי עיצובי כזה מוחל **על כל דפי פיתגורס** (שכבת נושא משותפת, לא חוק־עמוד מקומי כשהדבר אפשרי), ונאכף בבדיקות החוזה. אחרי כל תיקון מודדים בדפדפן: ‎ovY=0‎, ‏deadGap ≤ 40, אפס חיתוכים — ורק אז מדווחים.
- **דיוק גאומטרי במסיחים ובדוגמאות:** משולש שמוגדר ישר־זווית חייב למדוד 90° בפועל מתוך הקואורדינטות; מסיח שאמור להיות לא ישר־זווית לא יהיה בטעות 90° ולא יהיה „כמעט 90°” באופן שמייצר עמימות, אלא אם העמימות היא מושא השאלה במפורש. הבדיקה מחושבת, לא לפי העין.
- **קווי שרטוט דקים ונקיים להדפסה (הוראת יניב, 2026-08-18; הודק באותו יום):** קווי צלעות וסימוני זווית חייבים להיות חדים ועדינים. בשכבת היסוד ברירת המחדל היא בערך `1.5–1.7px` לצלעות ו־`1.1–1.4px` לסימוני עזר/זווית, בהתאמה ל־SVG. אין stroke שחור עבה שמכביד על דף מודפס. תוויות מידה בשרטוט נכתבות בכתב מתמטי ספרי — גופן סריפי, מספרים זקופים ומשתנים באיטליק — לא בגופן הממשק המודגש.
- **שאלות מגוונות:** הרצף כולל זיהוי וסימון, בחירה, השלמה, התאמה, חישוב, כתיבת משוואה, פתרון מלא, הבחנה בין יתר לניצב ושאלות משולבות. אין להפוך את כל דפי היסוד לאותו סוג תרגיל חוזר. הגיוון נעשה בין **מקבצי תרגול דידקטיים**, לא באמצעות פריט בודד מכל סוג; בתוך כל מיומנות חדשה חל כלל §4 „אין פריט בודד מסוג חדש”.
- **נימוק הוא חלק מהמענה (הוראת יניב, 2026-08-18):** כאשר מבקשים לזהות, לקבוע, לבחור או לסווג מושג מתמטי ונדרשת הבנה ולא רק סימון טכני, הכרטיס כולל מקום נפרד ל„נימוק” שמתאים לכתיבה ביד. ברירת המחדל היא לפחות שתי שורות שימושיות לנימוק קצר. תשובה קצרה אינה מחליפה את מקום הנימוק.
- **חזקות ושורשים לפני פיתגורס:** יש תרגול רחב של ריבועים, שורשים ופתרון \(x^2=n\). קודם מספרים עם שורש שלם, ורק אחר כך מספרים עם שורש שאינו שלם. כאשר \(x\) מייצג אורך צלע משתמשים בשורש החיובי בלבד; אין ללמד בטעות שהמשוואה האלגברית הכללית \(x^2=n\) כוללת רק פתרון חיובי מחוץ להקשר של אורך.
- **דוגמה לפני דרך חדשה:** לפני שהתלמיד נדרש לבצע סוג פתרון חדש מוצגת דוגמה מלאה, וכל מעבר נכתב בשורה נפרדת בלי דילוגים. לדוגמה בפתרון \(x^2=81\): \(x^2=81\) → \(x=\sqrt{81}\) → \(x=9\). השורה האחרונה היא התשובה הסופית וממוסגרת במלבן ברור.
- **דרך פתרון קנונית בפיתגורס:** בדוגמה למציאת יתר הדרך מוצגת כרצף שורות, למשל \(3^2+4^2=x^2\) → \(9+16=x^2\) → \(25=x^2\) → \(\sqrt{25}=x\) → \(5=x\). השורה האחרונה ממוסגרת. גם בתרגילים עצמם נשמר שטח כתיבה מספיק לכל שורות הדרך, ולא רק קו לתשובה.
- **קיבולת פתרון אמיתית (הוראת יניב, 2026-08-18; עודכן באותו יום):** בכל שאלה חישובית מלאה של פיתגורס דורשים **דרך מלאה שורה־שורה** ותיבת תשובה סופית נפרדת. אזור הפתרון הוא **משטח ריק ונקי — ללא שורות מודפסות** — בגובה שמספיק לכתיבת כל הדרך ביד. שאלה חישובית סטנדרטית עם שרטוט (מציאת ניצב/יתר) נארזת **6 בעמוד A4** בשני טורים; שאלה מורכבת במיוחד (מסובבת, רב־שלבית, אוריינית) רשאית לקבל פחות בעמוד. אין לכווץ שרטוט לגודל לא קריא כדי לדחוס; בדיקת A4 שעוברת ללא קיבולת כתיבה מספקת נחשבת כשל פדגוגי.
- **מספרים נקיים לפני מספרים לא־שלמים:** בתחילת כל מיומנות חדשה משתמשים בדוגמאות שנותנות ריבועים ושורשים שלמים כדי לבודד את הרעיון. רק לאחר שליטה בדרך מופיעים שורשים לא־שלמים, כתיב מדויק עם סימן שורש וקירוב עשרוני כשנדרש.
- **רצף הדפים:** דפי היסוד הנוכחיים מתחילים ב־`עמוד-634.html` וממשיכים בסדר הפדגוגי הקנוני שמוגדר ב־`meta/topics.json`, לא לפי סדר המספר הגלובלי בלבד. אחריהם ממשיכים הדפים הוותיקים. דפי פיצול חדשים מותרים ואף נדרשים כשאין מקום לדרך/נימוק; הם מקבלים זהות קובץ, מטא־דאטה, מספור מקומי וניווט קנוניים. אין לקבע כאן את המספר הכולל של דפי פיתגורס.
- **עיצוב ושרטוט:** A4, RTL ו־SVG וקטורי חד ובר־עריכה לפי §3–§4. שכבת היסודות המשותפת היא `styles/topics/pythagoras-foundations.css`; כל דף קנוני מקשר דרך `styles/pages/עמוד-N.css`. אין inline CSS.
- **שרשרת הניווט:** הדף שלפני פיתגורס מוביל אל דף היסודות הראשון; הדף האחרון של היסודות מוביל אל הדף הוותיק הראשון; משם הרצף הוותיק נמשך ללא שינוי בזהויות הקבצים. כאשר מוסיפים דף, בונים מחדש את המספור והקודם/הבא לפי הרצף המעודכן.
- **בדיקות:** הרצף, שמירת כל הדפים הוותיקים, כותרות המשפחה, השלמות עליונות משמעותיות, המושגים, כותרת עמוד יחידה, היעדר כותרות שאלה, היעדר רמז שמגלה תשובה, דיוק 90° ומסיחים, עובי שרטוט, סימן כפל בנקודה, נימוקים, דוגמאות הדרך, קיבולת כתיבה, מלבן התשובה הסופית, ה־footer, ה־RTL, היעדר inline CSS והמעבר לדפים הוותיקים נאכפים בבדיקות חוזה ייעודיות ובבדיקת דפדפן A4.

### 4.3 חוזה דיוק ואימות מדיד (הוראת יניב, 2026-07-19)

יניב דורש שימוש בטכנולוגיות החזקות והמדויקות ביותר שברשותי. אין להסתפק בהערכה חזותית או בניחוש.

- **כל גודל גאומטרי מחושב, לא מנוחש.** קואורדינטות של קרן, זווית, מצולע או קשת נגזרות מנוסחה מפורשת ונבדקות בבדיקת **הלוך־חזור**: מחשבים את הגודל בחזרה מהקואורדינטות שנכתבו לקובץ ומוודאים שהוא שווה בדיוק לערך המבוקש. סרטוט של „‎92.5°‎” חייב למדוד ‎92.5°‎ בפועל.
- **טקסט LTR ב־SVG חייב `direction="ltr"`.** בהקשר RTL האלגוריתם הדו־כיווני הופך תוכן שאינו עברי: `-5` מתרנדר `5-`, ו־`90°–100°` מתרנדר `100°9–0°`. הכלל: כל `<text>` שמכיל ספרה ואין בו אות עברית מקבל `direction="ltr"`.
- **סימון מתמטי נשמר בסדר LTR.** סימן שקודם לערך (למשל `∢` לפני האותיות) חייב להיות עטוף יחד עם מקום המענה בתוך אותו מכל `ltr`, אחרת ה־RTL ידחוף אותו לצד הלא נכון והתלמיד יקבל `ABC∢` במקום `∢ABC`.
- **אימות במדידה, לא בעין.** בדיקת סרטוטים מתבצעת בדפדפן אמיתי (Playwright): מיקומי תווים בפועל (`getStartPositionOfChar`) לגילוי היפוך, וחיתוך מדויק קטע־מול־מלבן בין תיבת הטקסט לכל stroke לגילוי התנגשות. ממצא חשוד נבדק ויזואלית בזום לפני תיקון — גלאי אוטומטי יכול לתת false positive (למשל `(0,5)` שנשמר תקין בזכות מראה דו־כיווני).
- **מקום המענה חייב להתאים למבנה התשובה הנדרשת** (הוראת יניב, 2026-07-19). תשובה מורכבת מקבלת **תיבה נפרדת לכל ערך**, עם האופרטור מודפס ביניהן — לא קו ארוך אחד. „כתבו את ‎∢ABC‎ כסכום של שתי זוויות” מקבל `▭ + ▭`; „כסכום של שלוש” מקבל `▭ + ▭ + ▭`; „כהפרש” מקבל `▭ − ▭`; מידות תיבה מקבלות `▭ · ▭ · ▭`; שיעורי נקודה מקבלים `( ▭ , ▭ )`; יחס מקבל `▭ : ▭`. המבנה הוא `.wexpr` (מכל `ltr`) עם `.wline` לכל ערך ו־`.wop` לאופרטור. **רק כשנדרש** — שאלה שתשובתה ערך יחיד או מילולית נשארת עם קו אחד; תיבה מיותרת מבלבלת בדיוק כמו תיבה חסרה.
- **דרך + תשובה סופית בכל שאלה חישובית** (הוראת יניב, 2026-07-19). שאלה שדורשת חישוב או פיתוח מקבלת **גם מקום לכתיבת הדרך** (שורות כתיבה / אזור עבודה) **וגם תיבה לתשובה הסופית** — לא רק אחד מהם. תיבת תשובה בלי שורת דרך לצידה היא פגם, ושורת דרך בלי תיבת תשובה סופית היא פגם. חריג: שאלה שהמענה שלה הוא סימון/מילוי בלבד (רב־ברירה, השלמת טבלה שהערך בה מיידי, סימון על סרטוט) אינה מחייבת שורות דרך; שאלת השלמת טבלה שדורשת חישוב מקבלת אזור דרך סמוך לטבלה. מספר שורות הדרך נגזר ממורכבות החישוב ומהמקום הפנוי בעמוד, בכפוף לכלל „שיפור מקבל את הרוחב שהוא צריך”.
- **שיפור מקבל את הרוחב שהוא צריך** (הוראת יניב, 2026-07-19). אם התוספת אינה נכנסת לדף — **מוסיפים דף עבודה**, לא מכווצים גופן, לא מוחקים תוכן ולא דוחסים מקום מענה. ניצול העמוד ‎(≥80%)‎ וחוזה A4 נשמרים תמיד; דף חדש מקבל את מספרו לפי §4.2 והנושא מתעדכן ב־`meta/topics.json`.
- **טרנספורמציה על HTML לא חוצה גבולות אלמנט.** רג'קס גלובלי על דף שלם חייב tempered greedy token (למשל `(?:(?!<svg)[\s\S])*?`) כדי שלא יבלע אלמנטים שכנים, ולפני כתיבה נבדקים **אינווריאנטים מבניים** (ספירת `<svg>`, `<div>`, `figure`, `figcap`). אינווריאנט שנשבר — לא כותבים כלום.

### 4.4 חוזה נושא „משוואות ריבועיות” — כותרות מקור + מאגר חדש מדורג

הוראה מפורשת של יניב (2026-07-19) לנושא זה בלבד:

- קובץ המקור „משוואות ריבועיות.pdf” משמש למיפוי הכותרות, סדר הנושאים, רמת כיתה ט' וספירת התרגילים בלבד. **אין להעתיק ממנו תרגיל** ואין לבצע החלפת מספרים מכנית; כל משוואה במאגר החדש מקורית.
- נשמרות 16 כותרות המקור ובאותו סדר. במקור 123 תרגילים; המאגר החדש מכיל **246 תרגילים בדיוק** — פי שניים — על פני 50 עמודי A4.
- ההתקדמות בתוך כל כותרת רציפה בארבע מדרגות פנימיות: ביסוס, שלב ביניים, שילוב כלים ואתגר. אם יש קפיצת קושי, נוספים תרגילי גשר לפני המעבר. המדרג קובע את סדר התרגילים בלבד; אין להציג לתלמיד רכיב "רמת התרגול", פסי התקדמות, שמות שלבי קושי או טווח תרגילים חזותי.
- העמוד הראשון בחוברת נפתח במשוואות יסוד מהצורה `ax^2=c` או `c=ax^2`, עם שני שורשים ממשיים שלמים; שלושה תרגילים מציבים את `x^2` משמאל ושלושה מימין. הוא כולל את הדוגמאות `2x^2=162`, `5x^2=125` ו־`-3=-3x^2`, ומסיים בשני תרגילים בלבד שבהם שני האגפים שליליים. בכל ששת התרגילים ערך `|x|` שונה כדי למנוע חזרה מכנית על אותה תשובה.
- העמוד השני הוא גשר מדורג של כינוס איברים דומים מהצורה `ax^2+bx^2=c`: ששת השורשים החיוביים עולים ממש, נכלל `x^2+x^2=162`, ורק בעמוד השלישי עוברים להוצאת `x` כגורם משותף במשוואות `c=0` בסיסיות ללא שברים או סוגריים.
- אין להציג כותרת שיטה נפרדת בכל כרטיס תרגיל (למשל „הוצאת גורם משותף”). השיטה נשמרת בנתונים לצרכים פדגוגיים פנימיים בלבד.
- המדרג תואם את תוכנית הלימודים לכיתה ט': משוואות חסרות, משוואות מלאות, סוגריים, ייצוג קדקודי, פרמטרים, מכנים מספריים, תחום הצבה, משוואות רציונליות ונוסחאות הכפל המקוצר. בשלבים המתקדמים נכללים גם שורשים אי־רציונליים ומקרים ללא פתרון ממשי.
- כל תרגיל כולל מרחב פתרון המתאים למורכבותו ותשובה בדוקה. במשוואה רציונלית מוצג מקום נפרד לתחום ההצבה; לשני שורשים מוצגות שתי תיבות נפרדות.
- מקור התוכן התחזוקתי הוא `sources/quadratic-equations/workbook-data.mjs`; הדפים הקנוניים נשארים `עמוד-N.html` ומופקים באמצעות `scripts/generate-quadratic-equations-workbook.mjs`.
- שכבת העיצוב המשותפת היא `styles/topics/quadratic-equations.css` בתחום `.quadratic-page`. היא שומרת על A4, RTL, MathJax, ניצול עמוד וקריאות בנייד לפי שאר חוזי הפרויקט.
- כל שינוי במאגר חייב לשמור על אינווריאנטים: 16 כותרות, 123 תרגילי מקור ממופים, 246 תרגילים חדשים, 50 עמודים, משוואות ייחודיות ומדרג קושי שאינו יורד בתוך כותרת.

### 4.5 חוזה החלוקה לפי תכנית הלימודים (הוראת יניב, 2026-07-19)

כל דפי העבודה בריפו מחולקים לפי תכנית הלימודים למתמטיקה בחטיבת הביניים: **כיתה → תחום → נושא → תת־נושא**.

- **מקור העץ:** `scripts/curriculum-map.mjs` מחזיק את הטקסונומיה (`CURRICULUM`), את הבית הראשי היחיד של כל דף (`PAGE_ASSIGNMENTS`) ואת ההצגות המשניות המאושרות (`PAGE_CROSS_LISTINGS`). זהו קובץ נתונים בלבד ואין בו כללי עבודה.
- **מקור התצוגה:** `scripts/build-curriculum.mjs` מזריק את העץ אל `meta/topics.json` תחת המפתח `curriculum`, מוסיף `curriculumId` לבית הראשי ו־`relatedCurriculumIds` להצגות משניות. מערך `topics` השטוח נשמר כפי שהוא ואינו נשבר.
- **שיוך ראשי והצגה משנית:** כל דף נרשם בדיוק פעם אחת ב־`PAGE_ASSIGNMENTS`. כאשר אותו דף שייך באופן אמיתי גם למסלול הוראה נוסף באותה שכת גיל, מותר להציגו שם דרך `PAGE_CROSS_LISTINGS` אלי לשכפל HTML ולבלנות את הבית הראשי.
- **אף מונה אינו נכתב ביד.** `directCount` מונה את הדפים המוצגים יישירים בצומת; `pageCount` מונה דפים ייחודיים בכל הענף, כי ש־cross-listing פנימי אינו מנפח את מונה האב. שני הערכים נגזרי ע׏ ידי `scripts/build-curriculum.mjs` ונבדקים מחדש ע׏ ידי `scripts/validate-curriculum.mjs`. אסור לכתוב מונה בקוד, בכל, ב־agent או בבדיקה.
- **כל צומת נשמר גם כשאין לו דפים** ("בית ריק"), כדי לשמר את תכנית הלימודים ולדעת מה עוד חסר. צומת ריק אינו נמחק מ־`scripts/curriculum-map.mjs` או מ־`meta/topics.json`, אך **אינו מוצג בניווט הציבורי** בקורא המחשב או בקורא הנייד. לוח המונים וכלי בדיקה רשאים להציגו לצורכי בקרה בלבד.
- **תוכן שאין לו בית בתכנית** נשמר תחת צומת `נושאים נוספים` המסומן `extension: true`. אסור להשמיט דף עבודה קיים רק משום שאין לו סעיף בתכנית.
- **אף דף לא נעלם:** דף שאין לו שיוך מוכר מוצג בקוראים תחת צומת גלוי „ממתינים לשיוך בתכנית הלימודים”. זו רשת ביטחון בלבד — `scripts/build-curriculum.mjs` נכשל במכוון על דף רשום ללא שיוך, כדי שכל דף חדש יקבל שיוך מפורש.
- **שערים:** `npm run curriculum:check` מוודא ש־`meta/topics.json` מעודכן מול המפה, ו־`npm run validate:curriculum` גוזר מחדש את כל המונים ומוודא שלכל דף יש בית ראשי אחד, שכל cross-listing חוקי ונשאר באותה שכבת גיל, שהמזהים ייחודיים ושאף קובץ בדיסק אינו חסר במטא־דאטה.
- **מזהי צמתים נגזרים זה מזה בנקודות** (`g7.geo.angles.bisector`), ולכן שרשרת האבות נקבעת מהמחרוזת עצמה. אין לשבור את המוסכמה הזו — הקוראים נשענים עליה לפתיחת מסלול הצומת הפעיל.
- **לוח מונים:** הקורא במחשב מציג לוח („📊 לוח מונים”) עם ספירת דפים לכל צומת בעץ, כולל הצמתים הריקים.

### 4.6 חוזה מרחב הפתרון ומיקום התשובה הסופית (הוראת יניב, 2026-07-22)

כלל גורף לכל דף עבודה בריפו, ובכל נושא. הוא מפרט בהקשר מדיד את §4.3 („דרך + תשובה סופית”, „מקום המענה חייב להתאים למבנה התשובה”, „שיפור מקבל את הרוחב שהוא צריך”).

- **מרחב הפתרון נמדד לפי מה שהתלמיד באמת צריך לכתוב**, לא לפי מה שנשאר פנוי בעמוד. תרגיל שדורש פיתוח אלגברי מלא מקבל אזור כתיבה שמספיק לכל שלבי הפתרון. אזור כתיבה שגובהו אינו מאפשר את הפתרון בפועל הוא פגם, גם אם הדף „נראה מלא”.
- **צפיפות מקסימלית לעמוד היא נגזרת של מרחב הפתרון, לא להפך.** קובעים תחילה כמה מקום כל תרגיל צריך, ורק אחר כך כמה תרגילים נכנסים לעמוד. **אם לא נכנס — מפצלים לדף עבודה נוסף** (§4.3). אסור לכווץ גופן, לדחוס את אזור הכתיבה או למחוק תוכן כדי „לחסוך” עמוד.
- **פיצול מייצר דפים אמיתיים ומלאים:** החלוקה מאוזנת (4→2+2, 5→3+2, 6→3+3) ולעולם לא משאירה עמוד עם שארית זעירה. כל דף חדש מקבל מספר גלובלי לפי §4.5, שיוך בתכנית הלימודים, קרדיט (§4), ומטא־דאטה מעודכן. המספור המקומי בנושא ושרשרת „הקודם/הבא” נבנים מחדש לכל הנושא (§2).
- **התשובה הסופית יושבת מיד מתחת לדרך של התלמיד, בצד שמאל** (הוראת יניב, 2026-07-30) — באותו כרטיס תרגיל, בשורה נפרדת שצמודה לתחתית אזור הכתיבה, בשורה האחרונה של הפתרון, מיושרת לצד שמאל של הדף (כיוון סיום הכתיבה המתמטית ב-LTR). אסור להציב אותה לצד התרגיל, לצד אזור הכתיבה, או במקום מנותק בעמוד. סדר הכרטיס הוא תמיד **תרגיל → אזור כתיבה → תשובה סופית**.
- **מבנה תיבות התשובה נגזר ממבנה התשובה** (§4.3): זוג סדור מקבל `( ▭ , ▭ )`, סכום מקבל `▭ + ▭`, וכן הלאה. התיבות נמצאות בתוך מכל `ltr` כדי שה־RTL לא יהפוך את סדרן.
- **סוגריים מסולסלות מייתרות את הכדור השחור** (הוראת יניב, 2026-07-30): תרגיל שפותח ב־`\begin{cases}` — הסוגר המסולסל הוא סימון התרגיל, ואין להוסיף לידו כדור שחור. הכדור השחור נשאר הסימון לשאלות ללא סוגר מסולסל (§4).
- **חשיבה של מורה אמיתי על כמות הכתיבה** (הוראת יניב, 2026-07-30): מספר התרגילים בעמוד נגזר מכמה התלמיד באמת כותב בסוג התרגיל, לא ממידה אחידה. תרגיל שבו נעלם כבר נתון או מבודד דורש שתי שורות — נכנסים 6 בעמוד בשני טורים (ימין ושמאל). תרגיל בידוד דורש יותר — 4 בעמוד. פתיחת סוגריים — 3. שברים ומכנה משותף — 2. אותו עיקרון חל על כל נושא בריפו.
- **גיוון אמיתי בתרגילים** (הוראת יניב, 2026-07-30): אין לחזור על אותו ערך נתון, אותו נעלם מבודד או אותה תבנית בתרגילים סמוכים. מגוונים את הנעלם (x/y), את מיקומו (משוואה ראשונה/שנייה) ואת הערכים.
- **אימות במדידה, לא בעין** (§4.3): נבדקים בדפדפן אמיתי גובה אזור הכתיבה בפועל, המרחק בין תחתית אזור הכתיבה לשורת התשובה, יישור התשובה לשמאל, ניצול העמוד ‎(≥80%)‎ והיעדר גלישה מ־A4.

**חוזה „מערכת משוואות בשני נעלמים”:** הכותרת היא שם הנושא בלבד; תת־הכותרת הקבועה היא **„פתרו ע"י הצבה”** — אין תוויות שלב כמו „פתיחה מדורגת” (§4, איסור תוויות דמו). קיבולת לפי כתיבה: נעלם נתון/מבודד — 6 בעמוד בשני טורים; בידוד — 4; סוגריים — 3; שברים — 2. אזור הפתרון הוא רשת משבצות שנמתחת על גובה העמוד (`grid-auto-rows: 1fr`); מתחתיו מיד שורת „תשובה סופית:” עם `( ▭ , ▭ )` בצד שמאל. כל מערכת מאומתת בפתרון יחיד על־ידי `scripts/lib/linear-system-solver.mjs` בזמן הבנייה. מקור התוכן: `sources/two-variable-systems/workbook-data.mjs`; המחולל: `scripts/generate-two-variable-systems.mjs` (‏`--check` הוא שער CI); החוזה נאכף ב־`tests/contracts/two-variable-systems-card-layout.test.mjs`.

---

## 5. תצוגה ושימוש בטלפון נייד — חוזה מחייב

### 5.1 כלל עליון

**כל מה שקיים באתר ובריפו חייב להיות זמין, גלוי, מובן ושמיש גם בנייד.**

Parity פירושו אותה יכולת אמיתית, לא בהכרח אותו סידור חזותי. כל נושא, דף, חיפוש, ניווט, הדפסה, PDF, הורדה, בחירה ומצב קריאה חייבים לקבל מסלול נייד ברור ובדוק.

### 5.2 טכנולוגיות נייד מחייבות

יש להשתמש ביכולות Web Platform היציבות והחזקות ביותר שמתאימות לצורך:

- CSS Grid/Flexbox ו־logical properties ל־RTL.
- `dvh`/`svh` עם fallback סביר.
- `env(safe-area-inset-*)` לכל אזור קבוע בקצוות.
- `VisualViewport` להתאמה למקלדת ולשינוי viewport אמיתי.
- `ResizeObserver` ו־`MutationObserver` להתייצבות A4, גופנים ו־MathJax, עם debounce וביטול עבודה כפולה.
- `IntersectionObserver` או virtualization אמיתי לרשימות/פרקים גדולים.
- Pointer/touch events ו־`touch-action` לפי הצורך.
- Web App Manifest, Service Worker ו־Cache API עבור PWA.
- progressive enhancement: כשל ב־API אופציונלי אינו מפיל את הקורא.
- Playwright ב־Chromium Android וב־WebKit iPhone לבדיקות קבועות.

אין להוסיף framework או ספרייה רק משום שהיא חדשה; בוחרים פתרון יציב, מהיר ותחזוקתי.

### 5.3 פריסה וקריאות

- אין גלישה אופקית של מעטפת האפליקציה בשום viewport נתמך.
- כל יעד מגע פעיל הוא לפחות `44×44px`, כולל זום, בחירה, סגירה וניווט.
- טקסט נייד חייב להיות בעברית ברורה, קצרה ומדויקת; אין קיצורים לא מובנים או כיתוב שנחתך.
- סרגל קבוע אינו מכסה סרגל אחר, תוכן, כפתור או safe area.
- תמיכה מחייבת לאורך, לרוחב, מסך קצר, notch, gesture bar, מקלדת פתוחה ו־standalone PWA.
- אין `min-height` או גובה קשיח שמכריח רכיב להמשיך מאחורי הניווט במסך לרוחב.
- `prefers-reduced-motion` נתמך.

### 5.4 קורא A4 בנייד

- הדף נשאר A4 קנוני ומוקטן חיצונית לפי רוחב וגובה המארח.
- במצב התאמה רגיל כל הדף נראה ללא גלילה אופקית.
- זום שומר על יחס A4 ומאפשר גלילה בתוך הדף המוגדל בלבד.
- שינוי orientation, viewport, גופן או MathJax מפעיל התאמה מחדש בצורה מתוזמנת ולא בלולאת layout.
- כשל בטעינת דף או היעדר `.a4-page` מוצגים למשתמש כמצב שגיאה אמיתי.

### 5.5 מצבי קריאה וניווט

- מצב עמוד בודד ומצב גלילה רציפה חייבים לחלוק מצב קנוני אחד של דף פעיל.
- בגלילה רציפה, הדף הקרוב לראש אזור הגלילה הופך לדף הנוכחי עבור כותרת, התקדמות ופעולות.
- מעבר נושא בונה מחדש את ערימת הגלילה לנושא החדש.
- קודם/הבא מושבתים במצב שבו הם אינם משנים את התצוגה בפועל.
- מעבר חזרה לעמוד בודד מפעיל התאמת scale מחדש.
- „פתח מלא” נשאר בתוך `mobile-app.html`, פותח את הדף המבוקש ושומר ניווט תקין בתוך הנושא.
- מצב full אינו משחזר בטעות מצב גלילה שמור.

### 5.6 ביצועים וזיכרון

- אין לטעון iframe לכל דפי פרק גדול בו־זמנית.
- גלילה רציפה משתמשת ב־virtualization ושומרת רק חלון קטן של דפים סמוכים טעונים.
- הדפסת פרק או בחירה גדולה מכינה iframes בקבוצות מוגבלות, לא במקביל ללא גבול.
- observers, timers, event listeners, object URLs ו־iframes מנוקים כאשר אינם נחוצים.
- אסור לנהל cache לפי אינסוף וריאציות query של אותו asset; יש מפתח cache קנוני.

### 5.7 בחירה ופעולות

- בחירת דף היא control נפרד ונגיש; אין interactive element מקונן בתוך button אחר.
- בחירות נשמרות בבטחה ומוצגות מיד גם לאחר רענון.
- סרגל הבחירה יושב מעל הניווט התחתון ואינו מכסה אותו.
- כל פעולת הדפסה/PDF/הורדה פועלת על הדף שמוצג בפועל.
- כשל ב־`localStorage` אינו מפיל את האפליקציה.

### 5.8 דיאלוגים ונגישות

- Dialog/Bottom sheet משתמש ב־`role="dialog"`,‏ `aria-modal`, שם נגיש ומיקוד נראה.
- בפתיחה: המיקוד עובר פנימה והרקע נהפך `inert`.
- Tab נשאר בתוך הדיאלוג, Escape וכפתור Back סוגרים אותו.
- בסגירה: המיקוד חוזר למפעיל והרקע חוזר לפעילות.
- אין timeout ישן שמסתיר דיאלוג שנפתח מחדש.

### 5.9 PWA, offline ועדכונים

- גרסת build אחת מופקת אוטומטית ומוזרקת ל־HTML, JS, manifest ו־Service Worker. אין מספרי cache ידניים בכמה קבצים.
- מעטפת הליבה ו־`meta/topics.json` עוברים precache.
- כשל `cache.put` אינו מבטל response תקין מהרשת.
- עדכון Service Worker אינו מערבב גרסה ישנה וחדשה: המשתמש מקבל הודעת עדכון, `skipWaiting` מופעל רק באישור, ו־`controllerchange` מבצע reload מתואם.
- במצב offline מוצג חיווי ברור ותוכן שנשמר נשאר שמיש.
- cache ישן של האפליקציה נמחק בהפעלה, בלי למחוק caches שאינם שייכים לה.

### 5.10 חוזה השלמה לכל שינוי נייד

תכונת מובייל אינה גמורה בלי כל אלה:

1. מימוש במסלול הקנוני.
2. בדיקת אינטראקציה למסע המשתמש החדש.
3. בדיקת layout ויעדי מגע.
4. בדיקת Android Chromium.
5. בדיקת iPhone WebKit כאשר ההתנהגות דפדפנית.
6. בדיקת portrait ו־landscape.
7. בדיקת מצב שמור ורענון כאשר יש state.
8. בדיקת PWA/offline/update כאשר השינוי נוגע להתקנה או cache.
9. בדיקת כל דפי A4 אם השינוי יכול להשפיע על הקורא.
10. צילום מסך ו־trace בכשל CI.

---

## 6. מטא־דאטה, build וגרסאות

- `meta/topics.json` חייב להתאים לכל קובצי `עמוד-N.html`, ללא כפילויות או קובץ חסר.
- אין ספירת דפים או נושאים קשיחה בכללים, agent או validator; הספירה נגזרת בזמן ריצה. מונה שנשמר ב־`meta/topics.json` הוא תוצר מחולל שנבדק מחדש בכל הרצה (§4.5), ואינו „ספירה קשיחה”.
- פלט audit נכתב ל־`meta/audit/` בלבד. אסור לסקריפט או ל־workflow לכתוב או להסתמך על `STATE/` מחוץ ל־`STATE/reports/`, ואסור לשמור דוח audit בגיט.
- build חייב להיכשל אם חסר asset קנוני או נשאר token שלא הוחלף.
- אין cache-busting ידני בכמה מקומות; version נוצר פעם אחת ב־build ומוזרק על-ידו. ה־build מטביע `?v=<buildVersion>` על סקריפט MathJax ועל כל קישורי ה־CSS בכל דפי `dist/` (`scripts/lib/versionize-assets.mjs`), כדי ששדרוג MathJax או סגנון לא ישאיר מנוע/CSS ישן ב־cache הדפדפן או ה־Service Worker שישבור תוכן חדש. טעינת MathJax היא self-hosted מ־`vendor/` בלבד — אין CDN.
- קובצי audit הם פלט בדיקה בלבד ואינם מקור דרישות.

---

## 7. בדיקות מחייבות

לפני release רלוונטי מריצים לפי היקף השינוי:

```bash
npm test
npm run verify
npm run validate:meta
npm run validate:schema
npm run curriculum:check
npm run validate:curriculum
npm run validate:access
npm run validate:mobile
npm run rules:check
npm run build
```

לכל שינוי נייד או שינוי layout:

```bash
npm run validate:mobile:browser
npm run validate:mobile:interactions
npm run validate:mobile:all-pages
```

`validate:mobile:all-pages` בודק כל דף בקורא וב־„פתח מלא” ב־`360×800`,‏ `412×915` ו־`915×412`. ב־CI הוא מפוצל ל־shards אך יחד מכסה את כל הדפים.

ה־CI חייב להפריד בין:

- geometry של כל דפי A4;
- interaction states של מעטפת המובייל;
- PWA/offline/update;
- build וחוזי הריפו.

בדיקה ירוקה בשכבה אחת אינה הוכחה שהשכבות האחרות תקינות.

---

## 8. Git ושינויים מסוכנים

- עובדים בענף ייעודי לשינוי רחב.
- בודקים diff ומבצעים commit ממוקד.
- אין `git add .`,‏ force push,‏ reset hard,‏ rebase או מחיקה רחבה בלי צורך ואישור מתאים.
- אין merge ל־`main` לפני שכל שערי ה־CI הרלוונטיים ירוקים.
- שינוי בקבצי A4, תוכן מתמטי או מקורות מוגנים חייב להישאר מחוץ לעבודת מעטפת מובייל, אלא אם המשתמש ביקש אותו במפורש.

---

## 9. ממשל תיעוד ו־AI

- agents ו־commands מגדירים רק שם תפקיד, תחום פעולה והפניה ל־`CLAUDE.md`.
- אסור להם להכיל Hard rules, ספירות, נתיבי מחשב מקומיים, רשימות בדיקות, מצב מערכת או הוראות Git עצמאיות.
- `README.md` מתאר את הפרויקט ומפנה לכאן; אינו משכפל חוזים.
- `docs/` מסביר ארכיטקטורה רק כאשר המידע אינו כלל וניתן להפיקו מהקוד; מסמך מיושן נמחק.
- אין `next-session`,‏ system-state ידני או זיכרון סשן שמלמד AI מצב ישן.
- `scripts/single-rules-source-check.mjs` חייב לסרוק את כל נקודות הכניסה ל־AI ולחסום מקור הוראות נוסף. הסריקה כוללת גם את `.github/instructions/`, את **כל** קובצי ה־Markdown בריפו (למעט `node_modules/`,‏ `dist/`,‏ `.git/`,‏ `sources/lovable/`), ואת כל הסקריפטים וה־workflows.
- כלי שאינו יכול לרוץ עוד — נתיב מכונה זר, הפניה ל־`PROJECT_RULES.md` או ל־`STATE/` — מסומן `DEPRECATED` בראשו ואינו נקרא ככלל עבודה, וכשהוא כבר אינו נחוץ הוא נמחק.

---

## 10. פתיחת כל סשן AI

1. קרא את `CLAUDE.md` במלואו.
2. בדוק branch,‏ `git status` ו־diff.
3. קרא `meta/topics.json` כאשר העבודה נוגעת לתוכן או ניווט.
4. קרא `package.json` לפני בחירת פקודות.
5. בדוק את המימוש והבדיקות הקיימים לפני יצירת פתרון חדש.
6. אמת במפורש את חוויית הטלפון בכל שינוי שמשפיע על UI, navigation, print, state או PWA.
